{% extends 'default.njk' %}
{% from 'macros.njk' import render_posts %} 

{% block content %}
    <header>
        <aside>
        </aside>
        <main>
            <h2>tags</h2>
        </main>
    </header>

    {% for tag, posts in tags %}
        <div class="tag">
            <header class="tag-header">
                <aside>
                    <h2><a href="/tags/{{ tag }}/">{{ tag }}</a></h2>
                </aside>
                <main>
                </main>
            </header>

            {{ render_posts(posts, '/tags/' + tag, false) }}
        </div>
    {% endfor %}
{% endblock %}
